# OS files
.DS_Store
.DS_Store?
._*
Thumbs.db
desktop.ini

# Editor / IDE
.vscode/
.idea/
*.sublime-project
*.sublime-workspace
*.swp
*~

# Environment & secrets (never commit credentials, keys, or LRS secrets)
.env
.env.*
*.local
secrets.*

# Logs
*.log
npm-debug.log*
yarn-error.log*

# Node / tooling (only relevant if a build is added later)
node_modules/
dist/
build/
.cache/

# Misc
*.tmp
*.bak
